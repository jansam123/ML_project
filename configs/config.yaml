data:
  path: /home/scur0034/ML_project/data/JetClass/Pythia/train_100M
  num_datasets: 700
  batch_size: 64
  num_workers: 8
  split:
    train: 0.7
    val: 0.15
    test: 0.15
  preprocess:
    normalize: true
    resize: [128, 128]

model:
  type: "model_type"
  hidden_dim: 256
  dropout: 0.2

training:
  lr: 0.001
  epochs: 3
  optimizer: adam