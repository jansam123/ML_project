data:
  path: /home/scur0034/ML_project/data/JetClass/Pythia/train_100M

  batch_size: 512
  num_workers: 8
  max_particles_in_jet: 128
  chunk_size: 32768
  samples_per_file: 8192

  split:
    train: 0.7
    val: 0.15
    test: 0.15

  seed: 42


model:
  name: ParticleTransformer

  # input dims
  particle_dim: 9
  jet_feature_dim: 9

  # core architecture
  hidden_dim: 128
  num_heads: 8
  num_layers: 4

  # regularization (reviewer requested dropout)
  dropout: 0.1

  # pooling choice (mean or max as requested)
  pooling_type: mean

  # optional future scaling
  transformer_dim: 128

  use_jet_features: True
  use_transformer: True


training:
  lr: 0.001
  weight_decay: 0.0001
<<<<<<< HEAD
  epochs: 10
=======
  epochs: 50
>>>>>>> origin/Lucas
  seed: 42

  scheduler:
    name: cosine
    min_lr: 0.00001

  # checkpointing
  monitor: val_loss
  monitor_mode: min